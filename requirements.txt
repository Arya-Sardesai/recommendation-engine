# Dependencies for the data pipeline (corpus build -> tag -> embed -> eval).
# This is NOT a venv freeze: it lists what the pipeline scripts actually import.
# The serving app has its own requirements in the hf-space repo.
#
# GPU note: embedding (embed_v1 / embed_movies / embed_tv) is run on a local
# GPU. The torch below installs the default (CPU) build, which is correct for
# Docker and for every non-embedding stage. For GPU embedding on the host,
# install the CUDA build per https://pytorch.org/get-started/locally/

pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
faiss-cpu==1.14.2
sentence-transformers==5.5.1
transformers==5.9.0
torch>=2.4
huggingface_hub==1.17.0
requests==2.34.2
rapidfuzz>=3.9
anthropic>=0.40
scikit-learn==1.8.0
tqdm==4.67.3
