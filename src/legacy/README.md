# Legacy (v0)

Pre-BGE-M3 apps kept for reference only. These use the old MiniLM embeddings
and round-robin recommender and are NOT the current version. The live app is
in the sibling hf-space repo; current agent code is in ../ (agent*.py).
Do not demo these.
